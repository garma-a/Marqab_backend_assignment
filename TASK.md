# المهمة التنفيذية

## العنوان

تحصين محرك الاستمرار الزمني ومعالجة الأحداث غير المرتبة.

## المدة

3 أيام عمل، بحد أقصى 24 ساعة تنفيذية.

المطلوب تنفيذ إصلاح فعلي داخل بنية هذه الحزمة، وليس إنشاء مشروع منفصل أو تقديم تقرير نظري.

## نموذج الحدث

```ts
type SignalEvent = {
  tenantId: string;
  scopeId: string;
  sourceId: string;
  signalCode: string;
  eventId: string;
  observedAt: string;
  receivedAt: string;
  value: number;
};
```

يجب فصل كل سلسلة وفق المفتاح:

```text
tenantId + scopeId + sourceId + signalCode
```

## القاعدة التجريبية

```json
{
  "ruleId": "RULE_ALPHA",
  "version": 1,
  "operator": "GT",
  "threshold": 70,
  "requiredDurationMs": 180000,
  "durationComparator": "GT",
  "maxGapMs": 60000,
  "allowedLatenessMs": 300000
}
```

## المطلوب تنفيذه

1. تشغيل المشروع والاختبارات الحالية وتوثيق حالتها قبل التعديل.
2. إصلاح المحرك بحيث تبدأ السلسلة عندما تكون القيمة أكبر من 70، بينما القيمة 70 نفسها غير مؤهلة وتقطع السلسلة.
3. يعتمد حساب المدة على `observedAt`، وليس `receivedAt` أو ترتيب وصول الطلبات.
4. عند استمرار الشرط 180 ثانية بالضبط لا تصدر الحالة المؤكدة، وتصدر فقط عند تجاوز 180 ثانية.
5. الفجوة الزمنية التي تساوي أو تقل عن 60 ثانية تبقي السلسلة متصلة، أما الفجوة 60.001 ثانية فتقطعها.
6. معالجة الأحداث التي تصل متأخرة أو بترتيب مختلف، وإعادة تقييم الجزء المتأثر من السلسلة بطريقة حتمية.
7. إذا غيّر حدث متأخر نتيجة سابقة:
   - لا يُحذف القرار السابق ولا يُعدّل.
   - يُنشأ قرار تصحيحي جديد يشير إلى القرار السابق.
   - تُحدّث الحالة الحالية من خلال سجل أو Projection منفصل.
8. ضمان منع التكرار على مستوى PostgreSQL:
   - تكرار `eventId` بالمحتوى نفسه يعامل كإعادة إرسال ناجحة.
   - تكرار `eventId` بمحتوى مختلف ينتج تعارضًا صريحًا ولا يستبدل السجل السابق.
   - وصول 20 طلبًا متزامنًا للحدث نفسه ينتج سجل حدث واحدًا وقرارًا واحدًا وأمر Outbox واحدًا كحد أقصى.
9. حفظ الأدلة التالية مع كل نتيجة:

```text
ruleId
ruleVersion
inputEventIds
windowStartedAt
windowEndedAt
outcome
decisionFingerprint
supersedesDecisionId
```

10. إضافة Replay يعيد تقييم الأحداث باستخدام إصدار القاعدة التاريخي نفسه، ويعطي النتيجة والبصمة نفسيهما دون إضافة أوامر تشغيلية مكررة.
11. يجب أن تأتي جميع الحدود من تعريف القاعدة، ولا تُكتب داخل الكود كقيم ثابتة.

في حجم بيانات التقييم الحالي، يجوز إعادة حساب السلسلة كاملة عند الحدث المتأخر إذا بقي التنفيذ حتميًا وصحيحًا. قصر إعادة الحساب على الجزء المتأثر وتحسين الأداء يُعدان ميزة إضافية، وليسا شرط نجاح مستقلًا خلال مدة المهمة.

## توضيح `allowedLatenessMs`

يُعرّف `watermark` بأنه أكبر `observedAt` مخزّن للسلسلة قبل إدخال الحدث الجديد:

- إذا كان `observedAt >= watermark` فالحدث ليس متأخرًا، ويُحدّث `watermark` عند الحاجة.
- إذا كان `observedAt < watermark` فالحدث متأخر.
- الحدث المتأخر مقبول لإعادة الحساب إذا كان `watermark - observedAt <= allowedLatenessMs`، بما في ذلك المساواة تمامًا.
- إذا كان الفرق أكبر من `allowedLatenessMs` فيُحفظ الحدث تاريخيًا بحالة `TOO_LATE`، لكنه لا يغيّر الحالة الحية أو يصدر أمرًا آليًا.

يعتمد ترتيب الحساب على `observedAt` تصاعديًا، ثم `eventId` كفاصل حتمي عند تساوي الوقت. `receivedAt` معلومة نقل وتدقيق فقط؛ لا تدخل في ترتيب الحساب أو بصمة القرار، ولا يُعد اختلافها وحده تعارضًا في محتوى إعادة الإرسال.

مفتاح Idempotency هو مفاتيح السلسلة الأربعة مع `eventId`. عند تطابق هذا المفتاح، تُقارن القيمتان غير القابلتين للتغيير `observedAt` و`value`: اختلاف إحداهما تعارض صريح، أما اختلاف `receivedAt` وحده فهو إعادة إرسال للمحتوى نفسه. ويُعد `eventId` نفسه داخل سلسلة مختلفة حدثًا مستقلًا لا تعارضًا.

يُقيّم المحرك السلسلة المؤهلة الحالية التي تنتهي عند أحدث حدث بحسب `observedAt`. أي قيمة غير مؤهلة أو فجوة تتجاوز `maxGapMs` تقطع السلسلة، ولا يجوز تأكيد الحالة الحالية اعتمادًا على سلسلة تاريخية انتهت قبل أحدث حدث.

## الاختبارات الإلزامية

- القيمة 70 لا تحقق الشرط وتقطع السلسلة.
- 180 ثانية بالضبط: لا تأكيد.
- 181 ثانية: تأكيد واحد.
- فجوة 60 ثانية: استمرار السلسلة.
- فجوة 60.001 ثانية: انقطاع السلسلة.
- وصول الأحداث بترتيب مختلف يعطي النتيجة النهائية نفسها.
- حدث متأخر يربط فجوة ويؤدي إلى إعادة تقييم صحيحة.
- حدث متأخر يقطع استمرارًا سبق تأكيده، مع حفظ القرار القديم وإضافة تصحيح.
- إرسال الحدث نفسه 20 مرة بالتوازي لا يسبب ازدواجًا.
- عدم دمج أحداث نطاقين أو مصدرين مختلفين.
- Replay متكرر يعيد النتيجة والبصمة نفسيهما.
- نشر نسخة ثانية من القاعدة لا يغيّر نتائج النسخة الأولى تاريخيًا.

يجب أن تشمل الاختبارات:

- Unit Tests للمنطق الزمني.
- Integration Tests باستخدام PostgreSQL حقيقية.
- اختبار تزامن فعلي، وليس Mocks فقط.

## التسليم

- Pull Request واحد محدود بهذه المهمة إذا تم توفير مستودع خاص وصلاحية وصول. وإذا استُلِمت الحزمة كملف ZIP فقط دون Remote، فيُسلّم مستودع Git جديد بتاريخ يبدأ من النسخة المستلمة، مع رابط خاص أو `git bundle` يوضح جميع التغييرات والـcommits.
- أي Migration أو قيود فريدة أو فهارس مطلوبة.
- جميع الاختبارات القديمة والجديدة ناجحة.
- شرح مختصر للخلل، والحل، وسياسة الأحداث المتأخرة ومنع التكرار وإعادة التشغيل.
- عرض مباشر للكود لمدة 30 دقيقة.
- أثناء العرض سيُطلب تغيير بسيط مباشر، مثل تحويل `GT` إلى `GTE` وتعديل الاختبار المرتبط به.

## خارج النطاق

- لا تعديلات على React أو أي واجهة مستخدم.
- لا إعادة بناء شاملة للسورس.
- لا تغييرات تجميلية أو خصائص غير مرتبطة بالمهمة.
- لا يُقبل تقرير أو مخطط دون كود واختبارات عاملة.
